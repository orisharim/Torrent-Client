services:
  pyqt5:
    build: .
    container_name: pyqt5-env
    environment:
      - DISPLAY=${DISPLAY}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
      - ./:/workspace
    working_dir: /workspace
    network_mode: host
    stdin_open: true
    tty: true
